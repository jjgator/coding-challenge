<%= form_for :comment, url: {action: "update", params: {id: @comment.id}} do |form| %>
    <%= form.label :comment %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.label :username %><br>
    <%= form.text_field :author %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>